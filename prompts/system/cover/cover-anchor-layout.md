You are designing a brand-new magazine-quality cover image for a Chinese WeChat article. Follow the layout contract below, then apply the style definition that comes after it.

LAYOUT CONTRACT (applies to every style)

- Safe area first: a framing note further down tells you how much of this canvas gets cut off. EVERY position word in this contract — top, bottom, corner, edge — refers to the surviving central band described there, NEVER to the raw canvas edge. Treat that band as the whole cover.
- No masthead: do NOT render any masthead, publication name, blogger name, brand name, account handle, logo or watermark anywhere on the cover. Purely decorative editorial marks with no name in them (thin rule lines, issue-number labels, page indexes, barcodes) are fine when the style calls for them.
- Headline (the soul of the cover): render the Chinese headline 「{{headline}}」 verbatim, exactly once, as the dominant visual element. No other large text anywhere on the canvas.
- Deck: below or beside the headline, render this short Chinese subtitle in one or two lines, verbatim: 「{{deck}}」. Use a much smaller, restrained type size. The headline column must never be left half-empty.
- Label tags: render these small Chinese label tags in the style's own decorative treatment (outlined chips, thin-rule labels, or the style's equivalent): {{tags}}. Keep them tiny and secondary.
- Core visual metaphor: {{metaphor}}
- Supporting picture elements: {{elements}}
- Composition for wide banners: when the canvas is a wide banner, use a left/right split — the text column (headline, deck, tags) occupies roughly the left 45%, the visual subject occupies the right 55%, and the two must not overlap. For square or portrait canvases, stack text above the visual instead.
- HARD RULE — bottom safe band: the bottom 10% of the canvas must stay completely clean. No text, no drawings, no icons, no decorative elements there — just a plain continuation of the background (light or dark are both fine).
- Text quality: all Chinese characters must be razor-sharp and correctly written. No garbled, invented or duplicated characters, no watermark, no stray English boilerplate, nothing cropped at the edges.

STYLE DEFINITION
